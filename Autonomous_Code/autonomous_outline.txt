Vision
  VuforiaRoverRuckus Vuforia
  VuforiaBase.TrackingResults vuFront
  VuforiaBase.TrackingResults vuBack
  VuforiaBase.TrackingResults vuRed
  VuforiaBase.TrackingResults vuBlue
  String markVis
  void init()
  void update()
  VuforiaBase.TrackingResults visible()
  void find(Navigation nav, VuforiaBase.TrackingResults vis)
  void open()
  void shut()

Hardware
  DcMotor rightMotor
  DcMotor leftMotor
  DcMotor liftMotor
  Servo dropServo
  Servo liftServo
  Servo clawServo
  void init()
  void motorStart(RobotConfig config)

RobotConfig
  double SPEED
  double CORRECTION
  double DISTANCE
  void set(double speed, double correction, double distance)

Navigation
  double xPos
  double yPos
  double rot
  void new()

Commodore_Autonomous
  void runOpMode()
