Vision
  VuforiaRoverRuckus Vuforia
  VuforiaBase.TrackingResults vuFront
  VuforiaBase.TrackingResults vuBack
  VuforiaBase.TrackingResults vuRed
  VuforiaBase.TrackingResults vuBlue
  String markVis
  double xPos
  double yPos
  double rot
  void init()
  void update()
  VuforiaBase.TrackingResults visible()
  void find(VuforiaBase.TrackingResults vis)
  void open()
  void shut()

Hardware
  DcMotor rightMotor
  DcMotor leftMotor
  DcMotor liftMotor
  Servo dropServo
  Servo liftServo
  Servo clawServo
  void init()
  void motorStart(RobotConfig config)
  void forward(RobotConfig config, double dist)
  void turn(RobotConfig config, double angle)

RobotConfig
  double SPEED
  double CORRECTION
  double FORWARD
  double TURN
  void set(double speed, double correction, double forward, double turn)

Commodore_Autonomous
  void runOpMode()
